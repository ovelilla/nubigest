import type { ReactNode } from "react";

type PublicLayoutProps = {
  children: ReactNode;
};

export type { PublicLayoutProps };

    contact: {
      label: string;
      email: string;
    };
    legal: {
      privacy: string;
      terms: string;
      refund: string;
      cancellation: string;
    };
    copyright: string;
  };
};

export type { PublicLayoutProps };
