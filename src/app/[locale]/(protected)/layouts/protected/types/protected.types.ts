type ProtectedLayoutProps = {
  children: React.ReactNode;
};

type ProtectedLayoutContainerProps = {
  children: React.ReactNode;
};

type ProtectedLayoutSidebarProps = {
  children: React.ReactNode;
};

type ProtectedLayoutHeaderProps = {
  children: React.ReactNode;
};

type ProtectedLayoutMainProps = {
  children: React.ReactNode;
};

export type {
  ProtectedLayoutProps,
  ProtectedLayoutContainerProps,
  ProtectedLayoutSidebarProps,
  ProtectedLayoutHeaderProps,
  ProtectedLayoutMainProps,
};
