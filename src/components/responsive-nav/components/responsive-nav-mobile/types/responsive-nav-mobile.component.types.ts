// Types
import type { Item } from "../../../types/responsive-nav.component.types";

type ResponsiveNavMobileProps = {
  items: Array<Item>;
  activeItem: Item;
};

export type { ResponsiveNavMobileProps };
