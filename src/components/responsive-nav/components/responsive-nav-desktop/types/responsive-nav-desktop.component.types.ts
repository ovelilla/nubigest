// Types
import type { Item } from "../../../types/responsive-nav.component.types";

type ResponsiveNavDesktopProps = {
  items: Array<Item>;
  activeItem: Item;
};

export type { ResponsiveNavDesktopProps };
